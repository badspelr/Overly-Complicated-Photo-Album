# =============================================================================# Django settings

# Photo Album Application - Environment Configuration TemplateDEBUG=True

# =============================================================================SECRET_KEY=your-secret-key-here

# Copy this file to .env and fill in your actual valuesALLOWED_HOSTS=localhost,127.0.0.1

# NEVER commit .env to git - it contains secrets!

## Database

# Quick setup: cp .env.example .envDB_ENGINE=django.db.backends.postgresql

# =============================================================================DB_NAME=photo_album

DB_USER=your-db-user

# =============================================================================DB_PASSWORD=your-db-password

# DJANGO SETTINGSDB_HOST=localhost

# =============================================================================DB_PORT=5432



# Generate a strong secret key with: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'# AI Features

SECRET_KEY=change-this-to-a-long-random-secret-key-do-not-use-in-productionAI_AUTO_TAGGING=True

AI_SMART_SEARCH=True

# Set to False in production!AI_DUPLICATE_DETECTION=True

DEBUG=TrueAI_CONTENT_MODERATION=False

AI_IMAGE_DESCRIPTIONS=True

# Comma-separated list of allowed host names (required when DEBUG=False)

# Example: ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com,app.yourdomain.com# Hugging Face API (optional, for higher rate limits)

ALLOWED_HOSTS=localhost,127.0.0.1# HF_TOKEN=your-huggingface-token-here



# =============================================================================# AI Models (using default models)

# DATABASE CONFIGURATIONAI_IMAGE_TAGGING_MODEL=microsoft/resnet-50

# =============================================================================AI_SEMANTIC_MODEL=openai/clip-vit-base-patch32

AI_TEXT_EMBEDDING_MODEL=all-MiniLM-L6-v2

DB_ENGINE=django.db.backends.postgresqlAI_NSFW_MODEL=Falconsai/nsfw_image_detection

DB_NAME=photo_album

DB_USER=postgres# AI Processing

DB_PASSWORD=change-this-secure-database-passwordAI_BATCH_SIZE=4

DB_HOST=dbAI_MAX_IMAGE_SIZE=1024

DB_PORT=5432AI_ENABLE_GPU=False

AI_CACHE_EMBEDDINGS=True

# =============================================================================

# SECURITY SETTINGS (Production Only)# Email (for production)

# =============================================================================# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend

# EMAIL_HOST=smtp.gmail.com

# Enable these in production with HTTPS:# EMAIL_PORT=587

SECURE_SSL_REDIRECT=False# EMAIL_HOST_USER=your-email@gmail.com

SESSION_COOKIE_SECURE=False# EMAIL_HOST_PASSWORD=your-email-password

CSRF_COOKIE_SECURE=False# EMAIL_USE_TLS=True

# SERVER_EMAIL=your-email@gmail.com

# =============================================================================# DEFAULT_FROM_EMAIL=your-email@gmail.com

# EMAIL CONFIGURATION

# =============================================================================# Security

SECURE_SSL_REDIRECT=False

# Email backend (console for dev, smtp for production)SESSION_COOKIE_SECURE=False

EMAIL_BACKEND=django.core.mail.backends.console.EmailBackendCSRF_COOKIE_SECURE=False



# SMTP settings (required for production)# File Upload

# Providers: Gmail, SendGrid, Mailgun, Amazon SES, etc.FILE_UPLOAD_MAX_MEMORY_SIZE=52428800

EMAIL_HOST=smtp.gmail.comDATA_UPLOAD_MAX_MEMORY_SIZE=104857600

EMAIL_PORT=587DATA_UPLOAD_MAX_NUMBER_FILES=1000
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-specific-password
EMAIL_USE_TLS=True

# From address for system emails
DEFAULT_FROM_EMAIL=noreply@yourdomain.com
SERVER_EMAIL=server@yourdomain.com

# Admin contact email (for contact form)
CONTACT_EMAIL=admin@yourdomain.com

# =============================================================================
# AI FEATURES (Optional - Hugging Face)
# =============================================================================

# Get your token from: https://huggingface.co/settings/tokens
# Required for AI image analysis and semantic search features
# Leave empty to disable AI features
HF_TOKEN=

# =============================================================================
# CELERY CONFIGURATION (Background Tasks)
# =============================================================================

CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/0
CELERY_WORKER_CONCURRENCY=4
CELERY_LOG_LEVEL=info

# =============================================================================
# GUNICORN CONFIGURATION (Production Web Server)
# =============================================================================

GUNICORN_WORKERS=2
GUNICORN_THREADS=2
GUNICORN_TIMEOUT=120

# =============================================================================
# FILE UPLOAD SETTINGS
# =============================================================================

# Maximum file size in bytes
# 10MB = 10485760, 50MB = 52428800, 100MB = 104857600
FILE_UPLOAD_MAX_MEMORY_SIZE=10485760
DATA_UPLOAD_MAX_MEMORY_SIZE=10485760
DATA_UPLOAD_MAX_NUMBER_FILES=1000

# =============================================================================
# RATE LIMITING (Optional)
# =============================================================================

RATELIMIT_ENABLE=True
RATELIMIT_USE_CACHE=default

# =============================================================================
# ADDITIONAL PRODUCTION SETTINGS
# =============================================================================

# Web server port (default: 8000)
WEB_PORT=8000

# Database port (default: 5432)
DB_PORT=5432

# Redis port (default: 6379)
REDIS_PORT=6379
