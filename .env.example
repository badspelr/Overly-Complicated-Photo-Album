# =============================================================================# =============================================================================# Django settings

# DJANGO PHOTO ALBUM - ENVIRONMENT CONFIGURATION

# =============================================================================# Photo Album Application - Environment Configuration TemplateDEBUG=True

# This is a template file for development. Copy to .env and customize.

# NEVER commit .env to git - it contains secrets!# =============================================================================SECRET_KEY=your-secret-key-here

#

# Quick setup: cp .env.example .env# Copy this file to .env and fill in your actual valuesALLOWED_HOSTS=localhost,127.0.0.1

# =============================================================================

# NEVER commit .env to git - it contains secrets!

# ⚠️ WARNING: THESE ARE DEVELOPMENT SETTINGS

# For production deployment, use .env.production as a template and ensure:## Database

#   - DEBUG=False

#   - SECURE_SSL_REDIRECT=True# Quick setup: cp .env.example .envDB_ENGINE=django.db.backends.postgresql

#   - SESSION_COOKIE_SECURE=True

#   - CSRF_COOKIE_SECURE=True# =============================================================================DB_NAME=photo_album

# See: docs/deployment/PRODUCTION_SECURITY_CHECKLIST.md

DB_USER=your-db-user

# =============================================================================

# DJANGO CORE SETTINGS# =============================================================================DB_PASSWORD=your-db-password

# =============================================================================

# DJANGO SETTINGSDB_HOST=localhost

# SECURITY WARNING: Generate a strong secret key for production

# Use: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'# =============================================================================DB_PORT=5432

SECRET_KEY=your-secret-key-here



# SECURITY WARNING: Set to False in production

DEBUG=True# Generate a strong secret key with: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'# AI Features



# Comma-separated list of allowed host names (required when DEBUG=False)SECRET_KEY=change-this-to-a-long-random-secret-key-do-not-use-in-productionAI_AUTO_TAGGING=True

ALLOWED_HOSTS=localhost,127.0.0.1

AI_SMART_SEARCH=True

# =============================================================================

# DATABASE CONFIGURATION# Set to False in production!AI_DUPLICATE_DETECTION=True

# =============================================================================

DEBUG=TrueAI_CONTENT_MODERATION=False

DB_ENGINE=django.db.backends.postgresql

DB_NAME=photo_albumAI_IMAGE_DESCRIPTIONS=True

DB_USER=your-db-user

DB_PASSWORD=your-db-password# Comma-separated list of allowed host names (required when DEBUG=False)

DB_HOST=db

DB_PORT=5432# Example: ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com,app.yourdomain.com# Hugging Face API (optional, for higher rate limits)



# =============================================================================ALLOWED_HOSTS=localhost,127.0.0.1# HF_TOKEN=your-huggingface-token-here

# REDIS CONFIGURATION

# =============================================================================



REDIS_HOST=redis# =============================================================================# AI Models (using default models)

REDIS_PORT=6379

REDIS_DB=0# DATABASE CONFIGURATIONAI_IMAGE_TAGGING_MODEL=microsoft/resnet-50



# =============================================================================# =============================================================================AI_SEMANTIC_MODEL=openai/clip-vit-base-patch32

# CELERY CONFIGURATION

# =============================================================================AI_TEXT_EMBEDDING_MODEL=all-MiniLM-L6-v2



CELERY_BROKER_URL=redis://redis:6379/0DB_ENGINE=django.db.backends.postgresqlAI_NSFW_MODEL=Falconsai/nsfw_image_detection

CELERY_RESULT_BACKEND=redis://redis:6379/0

DB_NAME=photo_album

# =============================================================================

# AI PROCESSING SETTINGSDB_USER=postgres# AI Processing

# =============================================================================

DB_PASSWORD=change-this-secure-database-passwordAI_BATCH_SIZE=4

# Enable/disable AI content moderation (NSFW detection)

AI_CONTENT_MODERATION=FalseDB_HOST=dbAI_MAX_IMAGE_SIZE=1024



# Device for AI processing: 'cuda' (GPU) or 'cpu'DB_PORT=5432AI_ENABLE_GPU=False

AI_DEVICE=cpu

AI_CACHE_EMBEDDINGS=True

# Model download cache directory (inside container)

TRANSFORMERS_CACHE=/app/models# =============================================================================



# =============================================================================# SECURITY SETTINGS (Production Only)# Email (for production)

# EMAIL CONFIGURATION (Development)

# =============================================================================# =============================================================================# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend



# Console backend prints emails to terminal (good for development)# EMAIL_HOST=smtp.gmail.com

EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

# Enable these in production with HTTPS:# EMAIL_PORT=587

# For production, use SMTP:

# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackendSECURE_SSL_REDIRECT=False# EMAIL_HOST_USER=your-email@gmail.com

# EMAIL_HOST=smtp.gmail.com

# EMAIL_PORT=587SESSION_COOKIE_SECURE=False# EMAIL_HOST_PASSWORD=your-email-password

# EMAIL_HOST_USER=your-email@gmail.com

# EMAIL_HOST_PASSWORD=your-email-passwordCSRF_COOKIE_SECURE=False# EMAIL_USE_TLS=True

# EMAIL_USE_TLS=True

# SERVER_EMAIL=your-email@gmail.com

DEFAULT_FROM_EMAIL=noreply@localhost

SERVER_EMAIL=server@localhost# =============================================================================# DEFAULT_FROM_EMAIL=your-email@gmail.com

CONTACT_EMAIL=contact@localhost

# EMAIL CONFIGURATION

# =============================================================================

# SECURITY SETTINGS (Development)# =============================================================================# Security

# =============================================================================

SECURE_SSL_REDIRECT=False

# ⚠️ DEVELOPMENT ONLY - These MUST be True in production with HTTPS configured

SECURE_SSL_REDIRECT=False# Email backend (console for dev, smtp for production)SESSION_COOKIE_SECURE=False

SESSION_COOKIE_SECURE=False

CSRF_COOKIE_SECURE=FalseEMAIL_BACKEND=django.core.mail.backends.console.EmailBackendCSRF_COOKIE_SECURE=False



# =============================================================================

# CORS CONFIGURATION

# =============================================================================# SMTP settings (required for production)# File Upload



# For development, allow localhost origins# Providers: Gmail, SendGrid, Mailgun, Amazon SES, etc.FILE_UPLOAD_MAX_MEMORY_SIZE=52428800

CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

EMAIL_HOST=smtp.gmail.comDATA_UPLOAD_MAX_MEMORY_SIZE=104857600

# Production: Use your actual domain

# CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.comEMAIL_PORT=587DATA_UPLOAD_MAX_NUMBER_FILES=1000

EMAIL_HOST_USER=your-email@gmail.com

# =============================================================================EMAIL_HOST_PASSWORD=your-app-specific-password

# FILE UPLOAD SETTINGSEMAIL_USE_TLS=True

# =============================================================================

# From address for system emails

# Maximum upload size in bytes (default: 50MB)DEFAULT_FROM_EMAIL=noreply@yourdomain.com

FILE_UPLOAD_MAX_MEMORY_SIZE=52428800SERVER_EMAIL=server@yourdomain.com



# =============================================================================# Admin contact email (for contact form)

# LOGGING CONFIGURATIONCONTACT_EMAIL=admin@yourdomain.com

# =============================================================================

# =============================================================================

# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL# AI FEATURES (Optional - Hugging Face)

LOG_LEVEL=INFO# =============================================================================



# =============================================================================# Get your token from: https://huggingface.co/settings/tokens

# OPTIONAL: GPU CONFIGURATION# Required for AI image analysis and semantic search features

# =============================================================================# Leave empty to disable AI features

HF_TOKEN=

# For GPU support, uncomment and configure:

# NVIDIA_VISIBLE_DEVICES=all# =============================================================================

# CUDA_VISIBLE_DEVICES=0# REDIS CONFIGURATION (Cache & Message Broker)

# =============================================================================

# =============================================================================

# OPTIONAL: CUSTOM DOMAIN# Redis URL for Django cache and Celery

# =============================================================================# Format: redis://[host]:[port]/[db_number]

# Database 0: Celery broker

# When deploying with a custom domain:# Database 1: Django default cache

# ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com# Database 2: AI embeddings cache

# CSRF_TRUSTED_ORIGINS=https://yourdomain.com,https://www.yourdomain.comREDIS_URL=redis://redis:6379/1



# =============================================================================# =============================================================================

# PRODUCTION DEPLOYMENT CHECKLIST# CELERY CONFIGURATION (Background Tasks)

# =============================================================================# =============================================================================

# Before deploying to production:

# CELERY_BROKER_URL=redis://redis:6379/0

# ✅ 1. Copy .env.production to .envCELERY_RESULT_BACKEND=redis://redis:6379/0

# ✅ 2. Generate NEW SECRET_KEY (never reuse development key)CELERY_WORKER_CONCURRENCY=4

# ✅ 3. Set DEBUG=FalseCELERY_LOG_LEVEL=info

# ✅ 4. Set SECURE_SSL_REDIRECT=True (requires HTTPS)

# ✅ 5. Set SESSION_COOKIE_SECURE=True (requires HTTPS)# =============================================================================

# ✅ 6. Set CSRF_COOKIE_SECURE=True (requires HTTPS)# CORS CONFIGURATION (Cross-Origin Resource Sharing)

# ✅ 7. Update ALLOWED_HOSTS with your domain# =============================================================================

# ✅ 8. Configure email (SMTP backend)

# ✅ 9. Use strong database password# SECURITY: List specific domains that can access your API

# ✅ 10. Configure CORS with your domain# Comma-separated list of allowed origins

## Example: CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# See: docs/deployment/PRODUCTION_SECURITY_CHECKLIST.mdCORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:8000,http://127.0.0.1:8000

# =============================================================================

# SECURITY WARNING: Never set to True in production!
# Setting this to True allows ANY website to access your API (major security vulnerability)
CORS_ALLOW_ALL_ORIGINS=False

# =============================================================================
# GUNICORN CONFIGURATION (Production Web Server)
# =============================================================================

GUNICORN_WORKERS=2
GUNICORN_THREADS=2
GUNICORN_TIMEOUT=120

# =============================================================================
# FILE UPLOAD SETTINGS
# =============================================================================

# Maximum file size in bytes
# 10MB = 10485760, 50MB = 52428800, 100MB = 104857600
FILE_UPLOAD_MAX_MEMORY_SIZE=10485760
DATA_UPLOAD_MAX_MEMORY_SIZE=10485760
DATA_UPLOAD_MAX_NUMBER_FILES=1000

# =============================================================================
# RATE LIMITING (Optional)
# =============================================================================

RATELIMIT_ENABLE=True
RATELIMIT_USE_CACHE=default

# =============================================================================
# ADDITIONAL PRODUCTION SETTINGS
# =============================================================================

# Web server port (default: 8000)
WEB_PORT=8000

# Database port (default: 5432)
DB_PORT=5432

# Redis port (default: 6379)
REDIS_PORT=6379
